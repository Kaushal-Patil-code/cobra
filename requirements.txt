# Runtime dependencies for COBRA — used by hosts that run
#   pip install -r requirements.txt   (e.g. Render's default Python builder).
# Pinned to the versions the test suite (111 passing) ran against, for
# reproducible deploys. Keep in sync with pyproject.toml (which uses >= floors).

# Web framework + WSGI server
flask==3.1.3
gunicorn==26.0.0

# Validation & typed settings
pydantic==2.13.4
pydantic-settings==2.14.1

# Config (.env loading)
python-dotenv==1.2.2

# Supabase Postgres (direct connection + pool)
psycopg[binary,pool]==3.3.4

# Fyers broker auto-login (TOTP)
fyers-apiv3==3.1.13
pyotp==2.10.0
requests==2.31.0

# Internal scheduler (self-driving tick + pre-login + Render keep-alive).
# Pulls tzlocal automatically (used for the IST cron timezone).
apscheduler==3.11.2
